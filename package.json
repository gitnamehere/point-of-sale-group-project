{
    "scripts": {
        "start": "cd app && node server.js",
        "setup": "psql -U postgres -f setup.sql",
        "setup-demo": "psql -U postgres -f setup.sql && psql -U postgres -f demo-data.sql",
        "demo-only": "psql -U postgres -f demo-data.sql",
        "prettier": "npx prettier . --write"
    },
    "dependencies": {
        "express": "^4.19.2",
        "pg": "^8.12.0"
    },
    "devDependencies": {
        "prettier": "3.3.3"
    }
}
